# ============================================================================
# PAN AUDIO - ENVIRONMENT VARIABLES
# ============================================================================

# ----------------------------------------------------------------------------
# FRONTEND VARIABLES (VITE)
# ----------------------------------------------------------------------------
# Development: http://localhost:5000
# Production: https://your-app.up.railway.app or https://your-app.fly.dev
VITE_API_URL=http://localhost:5000

# ----------------------------------------------------------------------------
# BACKEND VARIABLES (See server/.env.example for backend config)
# ----------------------------------------------------------------------------
# Database, Cloudinary, JWT secrets are configured in server/.env
